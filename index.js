var customerName = "bob";

function upperCaseCustomerName(customerName) {
  const bigName = customerName.toUpperCase();
  return bigName;
}

function setBestCustomer() {
  var bestCustomer = "not bob";
}
